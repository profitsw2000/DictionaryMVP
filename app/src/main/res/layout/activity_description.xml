<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/description_screen_swipe_refresh_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/refresh_layout_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/description_linear_layout"
        android:orientation="vertical"
        android:padding="@dimen/refresh_layout_padding"
        android:fitsSystemWindows="true">

        <TextView
            android:id="@+id/translation_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/margin_large"
            android:textColor="@color/black"
            android:textSize="@dimen/header_text_view_size"
            tools:text="Text"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/main_recycler_view_note_text_view"
            android:textColor="@color/design_default_color_primary_dark"
            android:textSize="@dimen/translation_text_view_size"
            android:layout_margin="@dimen/margin_large"
            android:alpha="0.6"
            tools:text="Текст"
            />
        
        <ru.profitsw2000.dictionarymvp.utils.EquilateralImageView
            android:id="@+id/description_image_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/margin_medium"
            android:layout_gravity="center_horizontal"
            app:srcCompat="@drawable/ic_no_photo_vector"
            android:visibility="visible"
            tools:src="@drawable/ic_no_photo_vector"
            tools:visibility="visible"/>

    </LinearLayout>
</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>